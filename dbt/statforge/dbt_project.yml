name: statforge
version: "1.0.0"
config-version: 2

profile: statforge

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  statforge:
    staging:
      +materialized: view
    marts:
      +materialized: table
